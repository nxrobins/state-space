[
  {
    "id": "gen15_5503",
    "generation": 15,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen5_5150"
    ],
    "mutation_description": "based phase transitions. Parent 1 uses bitwise priority masks, Parent 2 optimizes for single transitions with countOneBi",
    "code_diff": "based phase transitions. Parent 1 uses bitwise priority masks, Parent 2 optimizes for single transitions with countOneBi",
    "fitness": {
      "energy_conservation_error": Infinity,
      "matter_conservation_error": Infinity,
      "determinism_score": 0.0,
      "tick_time_ms": Infinity,
      "diverged": true,
      "error": "Validation Error\n\nCaused by:\n  In wgpuDeviceCreateShaderModule\n\nShader validation error: Entry point tick at Compute is invalid\n    \u250c\u2500 :187:20\n    \u2502\n187 \u2502     let hot_mask = vec2<u32>(thermal_vec.x > points.x, thermal_vec.x > points.y); // melt, boil checks\n    \u2502                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ naga::ir::Expression [46]\n    \u2502\n    = Expression [46] is invalid\n    = Composing 0's component type is not expected\n\n\n      Expression [46] is invalid\n        Composing 0's component type is not expected",
      "disqualified": true,
      "disqualification_reason": "Shader compilation or pipeline creation failed"
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:51:43.666642",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points as vec4 for SIMD\n    let points = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u)); // melt, boil, freeze, condense\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u)); // melts_into, boils_into, freezes_into, condenses_into\n    \n    // Build thermal comparison masks\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_mask = vec2<u32>(thermal_vec.x > points.x, thermal_vec.x > points.y); // melt, boil checks\n    let cold_mask = vec2<u32>(thermal_vec.x < points.z, thermal_vec.x < points.w); // freeze, condense checks\n    \n    // Combine into transition availability (target != 0 && point != 0 && thermal condition met)\n    let points_valid = vec4<u32>(points.x != 0u, points.y != 0u, points.z != 0u, points.w != 0u);\n    let targets_valid = vec4<u32>(targets.x != 0u, targets.y != 0u, targets.z != 0u, targets.w != 0u);\n    let thermal_ok = vec4<u32>(hot_mask.x, hot_mask.y, cold_mask.x, cold_mask.y);\n    \n    let can_transition = points_valid & targets_valid & thermal_ok;\n    \n    // Pack into bitfield: boil=8, melt=4, freeze=2, condense=1\n    let transitions = (can_transition.y << 3u) | (can_transition.x << 2u) | \n                     (can_transition.z << 1u) | can_transition.w;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority transition using bit scan\n    // For 4-bit field, we can use a simple priority cascade\n    let priority_idx = select(\n        select(\n            select(0u, 1u, (transitions & 1u) != 0u),  // condense\n            2u, (transitions & 2u) != 0u               // freeze\n        ),\n        select(3u, 4u, (transitions & 8u) != 0u),      // melt or boil\n        (transitions & 12u) != 0u                      // any hot transition\n    );\n    \n    // Extract winning transition using computed index\n    // Map: 0=none, 1=condense, 2=freeze, 3=melt, 4=boil\n    let winner_mask = vec4<u32>(\n        priority_idx == 3u,  // melt\n        priority_idx == 4u,  // boil\n        priority_idx == 2u,  // freeze\n        priority_idx == 1u   // condense\n    );\n    \n    // Compute new material using dot product\n    let new_mat = dot(targets, winner_mask);\n    \n    // Load latent heats and compute thermal change\n    let lh_melt = ct(mat, 10u);\n    let lh_boil = ct(mat, 11u);\n    let thermal_cost = (lh_melt * winner_mask.x) | (lh_boil * winner_mask.y);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Compute new phase\n    let phase_map = vec4<u32>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = dot(phase_map, winner_mask);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Phase transition with precomputed transition masks and vectorized operations\n// Uses vec4 operations to batch-process transition conditions and minimize instruction count\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points as vec4 for SIMD\n    let points = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u)); // melt, boil, freeze, condense\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u)); // melts_into, boils_into, freezes_into, condenses_into\n    \n    // Build thermal comparison masks\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_mask = vec2<u32>(thermal_vec.x > points.x, thermal_vec.x > points.y); // melt, boil checks\n    let cold_mask = vec2<u32>(thermal_vec.x < points.z, thermal_vec.x < points.w); // freeze, condense checks\n    \n    // Combine into transition availability (target != 0 && point != 0 && thermal condition met)\n    let points_valid = vec4<u32>(points.x != 0u, points.y != 0u, points.z != 0u, points.w != 0u);\n    let targets_valid = vec4<u32>(targets.x != 0u, targets.y != 0u, targets.z != 0u, targets.w != 0u);\n    let thermal_ok = vec4<u32>(hot_mask.x, hot_mask.y, cold_mask.x, cold_mask.y);\n    \n    let can_transition = points_valid & targets_valid & thermal_ok;\n    \n    // Pack into bitfield: boil=8, melt=4, freeze=2, condense=1\n    let transitions = (can_transition.y << 3u) | (can_transition.x << 2u) | \n                     (can_transition.z << 1u) | can_transition.w;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority transition using bit scan\n    // For 4-bit field, we can use a simple priority cascade\n    let priority_idx = select(\n        select(\n            select(0u, 1u, (transitions & 1u) != 0u),  // condense\n            2u, (transitions & 2u) != 0u               // freeze\n        ),\n        select(3u, 4u, (transitions & 8u) != 0u),      // melt or boil\n        (transitions & 12u) != 0u                      // any hot transition\n    );\n    \n    // Extract winning transition using computed index\n    // Map: 0=none, 1=condense, 2=freeze, 3=melt, 4=boil\n    let winner_mask = vec4<u32>(\n        priority_idx == 3u,  // melt\n        priority_idx == 4u,  // boil\n        priority_idx == 2u,  // freeze\n        priority_idx == 1u   // condense\n    );\n    \n    // Compute new material using dot product\n    let new_mat = dot(targets, winner_mask);\n    \n    // Load latent heats and compute thermal change\n    let lh_melt = ct(mat, 10u);\n    let lh_boil = ct(mat, 11u);\n    let thermal_cost = (lh_melt * winner_mask.x) | (lh_boil * winner_mask.y);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Compute new phase\n    let phase_map = vec4<u32>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = dot(phase_map, winner_mask);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen16_5536",
    "generation": 16,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen5_5150"
    ],
    "mutation_description": "in functions.",
    "code_diff": "in functions.",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.25528499827487394,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:52:16.322115",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition thresholds as vec4\n    let thresholds_hot = vec4<u32>(ct(mat, 2u), ct(mat, 3u), 0u, 0u);  // melt, boil\n    let thresholds_cold = vec4<u32>(ct(mat, 8u), ct(mat, 9u), 0u, 0u); // freeze, condense\n    \n    // Load latent heats\n    let latent_heats = vec2<u32>(ct(mat, 10u), ct(mat, 11u)); // melt, boil\n    \n    // Load target materials as vec4\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Broadcast thermal for comparison\n    let thermal_vec = vec4<u32>(thermal);\n    \n    // Compute transition conditions\n    let hot_cond = vec2<u32>(\n        u32((targets.x != 0u) && (thresholds_hot.x != 0u) && (thermal > thresholds_hot.x)),\n        u32((targets.y != 0u) && (thresholds_hot.y != 0u) && (thermal > thresholds_hot.y))\n    );\n    \n    let cold_cond = vec2<u32>(\n        u32((targets.z != 0u) && (thresholds_cold.x != 0u) && (thermal < thresholds_cold.x)),\n        u32((targets.w != 0u) && (thresholds_cold.y != 0u) && (thermal < thresholds_cold.y))\n    );\n    \n    // Build priority field: boil=8, melt=4, freeze=2, condense=1\n    let transition_bits = (hot_cond.y << 3u) | (hot_cond.x << 2u) | \n                         (cold_cond.x << 1u) | cold_cond.y;\n    \n    if (transition_bits == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Find highest priority transition using firstLeadingBit\n    let priority_bit = firstLeadingBit(transition_bits);\n    \n    // Create selection masks\n    let mask_boil = u32(priority_bit == 3u);\n    let mask_melt = u32(priority_bit == 2u);\n    let mask_freeze = u32(priority_bit == 1u);\n    let mask_condense = u32(priority_bit == 0u);\n    \n    // Select new material\n    let new_mat = (targets.y * mask_boil) | \n                  (targets.x * mask_melt) | \n                  (targets.z * mask_freeze) | \n                  (targets.w * mask_condense);\n    \n    // Compute thermal change\n    let thermal_cost = (latent_heats.y * mask_boil) | (latent_heats.x * mask_melt);\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), thermal_cost != 0u);\n    \n    // Select new phase\n    let new_phase = (PHASE_GAS * mask_boil) | \n                    (PHASE_LIQUID * (mask_melt | mask_condense)) | \n                    (PHASE_FROZEN * mask_freeze);\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Vectorized transition logic with branchless material selection\n// Uses vec4 operations to evaluate all transitions in parallel and firstLeadingBit for priority\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition thresholds as vec4\n    let thresholds_hot = vec4<u32>(ct(mat, 2u), ct(mat, 3u), 0u, 0u);  // melt, boil\n    let thresholds_cold = vec4<u32>(ct(mat, 8u), ct(mat, 9u), 0u, 0u); // freeze, condense\n    \n    // Load latent heats\n    let latent_heats = vec2<u32>(ct(mat, 10u), ct(mat, 11u)); // melt, boil\n    \n    // Load target materials as vec4\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Broadcast thermal for comparison\n    let thermal_vec = vec4<u32>(thermal);\n    \n    // Compute transition conditions\n    let hot_cond = vec2<u32>(\n        u32((targets.x != 0u) && (thresholds_hot.x != 0u) && (thermal > thresholds_hot.x)),\n        u32((targets.y != 0u) && (thresholds_hot.y != 0u) && (thermal > thresholds_hot.y))\n    );\n    \n    let cold_cond = vec2<u32>(\n        u32((targets.z != 0u) && (thresholds_cold.x != 0u) && (thermal < thresholds_cold.x)),\n        u32((targets.w != 0u) && (thresholds_cold.y != 0u) && (thermal < thresholds_cold.y))\n    );\n    \n    // Build priority field: boil=8, melt=4, freeze=2, condense=1\n    let transition_bits = (hot_cond.y << 3u) | (hot_cond.x << 2u) | \n                         (cold_cond.x << 1u) | cold_cond.y;\n    \n    if (transition_bits == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Find highest priority transition using firstLeadingBit\n    let priority_bit = firstLeadingBit(transition_bits);\n    \n    // Create selection masks\n    let mask_boil = u32(priority_bit == 3u);\n    let mask_melt = u32(priority_bit == 2u);\n    let mask_freeze = u32(priority_bit == 1u);\n    let mask_condense = u32(priority_bit == 0u);\n    \n    // Select new material\n    let new_mat = (targets.y * mask_boil) | \n                  (targets.x * mask_melt) | \n                  (targets.z * mask_freeze) | \n                  (targets.w * mask_condense);\n    \n    // Compute thermal change\n    let thermal_cost = (latent_heats.y * mask_boil) | (latent_heats.x * mask_melt);\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), thermal_cost != 0u);\n    \n    // Select new phase\n    let new_phase = (PHASE_GAS * mask_boil) | \n                    (PHASE_LIQUID * (mask_melt | mask_condense)) | \n                    (PHASE_FROZEN * mask_freeze);\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen17_5579",
    "generation": 17,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen5_5150"
    ],
    "mutation_description": "1. Parent 1: Bitwise masking with priority computation",
    "code_diff": "1. Parent 1: Bitwise masking with priority computation",
    "fitness": {
      "energy_conservation_error": Infinity,
      "matter_conservation_error": Infinity,
      "determinism_score": 0.0,
      "tick_time_ms": Infinity,
      "diverged": true,
      "error": "Validation Error\n\nCaused by:\n  In wgpuDeviceCreateShaderModule\n\nShader '' parsing error: expected `)`, found \"6u\"\n    \u250c\u2500 wgsl:218:59\n    \u2502\n218 \u2502                   ((targets_lo.z >> (mat_idx << 3u)) << 1u6u) |\n    \u2502                                                           ^^ expected `)`\n\n",
      "disqualified": true,
      "disqualification_reason": "Shader compilation or pipeline creation failed"
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:52:59.871927",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters as vec4s for efficiency\n    let points_lo = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let latent_heat = vec2<u32>(ct(mat, 10u), ct(mat, 11u));\n    let targets_lo = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n\n    // Build transition bitfield (boil=8, melt=4, freeze=2, condense=1)\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = vec2<u32>(thermal_vec.x > points_lo.x, thermal_vec.x > points_lo.y);\n    let cold_checks = vec2<u32>(thermal_vec.x < points_lo.z, thermal_vec.x < points_lo.w);\n    let target_checks = vec4<u32>(targets_lo.x != 0u, targets_lo.y != 0u, targets_lo.z != 0u, targets_lo.w != 0u);\n    let point_checks = vec4<u32>(points_lo.x != 0u, points_lo.y != 0u, points_lo.z != 0u, points_lo.w != 0u);\n    \n    let transitions = ((u32(target_checks.y & point_checks.y & hot_checks.y) << 3u) |\n                      (u32(target_checks.x & point_checks.x & hot_checks.x) << 2u) |\n                      (u32(target_checks.z & point_checks.z & cold_checks.x) << 1u) |\n                      u32(target_checks.w & point_checks.w & cold_checks.y));\n\n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: maps 4-bit transition field to 2-bit winner\n    // Encoding: 00=condense, 01=freeze, 10=melt, 11=boil\n    // Priority: boil > melt > freeze > condense\n    const PRIORITY_LUT: u32 = 0xFFAACC00u;  // Each 2 bits encode winner for that transition combo\n    \n    // Extract 2-bit winner from lookup table\n    let winner = (PRIORITY_LUT >> (transitions << 1u)) & 3u;\n    \n    // Decode winner to material index using multiply-add\n    // winner: 0->3, 1->2, 2->0, 3->1 (maps to condense/freeze/melt/boil indices)\n    let mat_idx = (winner * 3u + 2u) & 3u;\n    \n    // Extract new material from targets using bit manipulation\n    let new_mat = (targets_lo.x >> (mat_idx << 3u)) |\n                  ((targets_lo.y >> (mat_idx << 3u)) << 8u) |\n                  ((targets_lo.z >> (mat_idx << 3u)) << 16u) |\n                  ((targets_lo.w >> (mat_idx << 3u)) << 24u);\n    let selected_mat = (new_mat >> ((3u - mat_idx) << 3u)) & 0xFFu;\n    \n    // Compute thermal change (only for melt=2 or boil=3)\n    let is_phase_up = winner >> 1u;  // 1 for melt/boil, 0 for freeze/condense\n    let heat_idx = winner & 1u;      // 0 for melt, 1 for boil\n    let thermal_cost = select(0u, select(latent_heat.x, latent_heat.y, heat_idx != 0u), is_phase_up != 0u);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Compute phase: 0=frozen, 1=liquid, 2=gas (condense->liquid)\n    let phase_map = 0x00010201u;  // Maps winner to phase: condense->1, freeze->0, melt->1, boil->2\n    let new_phase = (phase_map >> (winner << 3u)) & 0xFFu;\n\n    grid_out[idx] = rewrite_voxel(voxel, selected_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Phase transition via 16-entry lookup table encoded in a single u32\n// Maps 4-bit transition field directly to winner without branches\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters as vec4s for efficiency\n    let points_lo = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let latent_heat = vec2<u32>(ct(mat, 10u), ct(mat, 11u));\n    let targets_lo = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n\n    // Build transition bitfield (boil=8, melt=4, freeze=2, condense=1)\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = vec2<u32>(thermal_vec.x > points_lo.x, thermal_vec.x > points_lo.y);\n    let cold_checks = vec2<u32>(thermal_vec.x < points_lo.z, thermal_vec.x < points_lo.w);\n    let target_checks = vec4<u32>(targets_lo.x != 0u, targets_lo.y != 0u, targets_lo.z != 0u, targets_lo.w != 0u);\n    let point_checks = vec4<u32>(points_lo.x != 0u, points_lo.y != 0u, points_lo.z != 0u, points_lo.w != 0u);\n    \n    let transitions = ((u32(target_checks.y & point_checks.y & hot_checks.y) << 3u) |\n                      (u32(target_checks.x & point_checks.x & hot_checks.x) << 2u) |\n                      (u32(target_checks.z & point_checks.z & cold_checks.x) << 1u) |\n                      u32(target_checks.w & point_checks.w & cold_checks.y));\n\n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: maps 4-bit transition field to 2-bit winner\n    // Encoding: 00=condense, 01=freeze, 10=melt, 11=boil\n    // Priority: boil > melt > freeze > condense\n    const PRIORITY_LUT: u32 = 0xFFAACC00u;  // Each 2 bits encode winner for that transition combo\n    \n    // Extract 2-bit winner from lookup table\n    let winner = (PRIORITY_LUT >> (transitions << 1u)) & 3u;\n    \n    // Decode winner to material index using multiply-add\n    // winner: 0->3, 1->2, 2->0, 3->1 (maps to condense/freeze/melt/boil indices)\n    let mat_idx = (winner * 3u + 2u) & 3u;\n    \n    // Extract new material from targets using bit manipulation\n    let new_mat = (targets_lo.x >> (mat_idx << 3u)) |\n                  ((targets_lo.y >> (mat_idx << 3u)) << 8u) |\n                  ((targets_lo.z >> (mat_idx << 3u)) << 16u) |\n                  ((targets_lo.w >> (mat_idx << 3u)) << 24u);\n    let selected_mat = (new_mat >> ((3u - mat_idx) << 3u)) & 0xFFu;\n    \n    // Compute thermal change (only for melt=2 or boil=3)\n    let is_phase_up = winner >> 1u;  // 1 for melt/boil, 0 for freeze/condense\n    let heat_idx = winner & 1u;      // 0 for melt, 1 for boil\n    let thermal_cost = select(0u, select(latent_heat.x, latent_heat.y, heat_idx != 0u), is_phase_up != 0u);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Compute phase: 0=frozen, 1=liquid, 2=gas (condense->liquid)\n    let phase_map = 0x00010201u;  // Maps winner to phase: condense->1, freeze->0, melt->1, boil->2\n    let new_phase = (phase_map >> (winner << 3u)) & 0xFFu;\n\n    grid_out[idx] = rewrite_voxel(voxel, selected_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen18_5615",
    "generation": 18,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen5_5150"
    ],
    "mutation_description": "```wgsl",
    "code_diff": "```wgsl",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.15673999732825905,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:53:35.311847",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield (boil=8, melt=4, freeze=2, condense=1)\n    let transitions = (u32(boils_into != 0u && boil_point != 0u && thermal > boil_point) << 3u) |\n                     (u32(melts_into != 0u && melt_point != 0u && thermal > melt_point) << 2u) |\n                     (u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point) << 1u) |\n                     u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: for each 4-bit transition pattern, store the winning bit position\n    // Priority order: boil(3) > melt(2) > freeze(1) > condense(0)\n    // Index is the transition bitfield, value is the bit position of winner\n    let priority_lut = array<u32, 16>(\n        0u, 0u, 1u, 1u,  // 0000, 0001, 0010, 0011\n        2u, 2u, 2u, 2u,  // 0100, 0101, 0110, 0111\n        3u, 3u, 3u, 3u,  // 1000, 1001, 1010, 1011\n        3u, 3u, 3u, 3u   // 1100, 1101, 1110, 1111\n    );\n    \n    let winner = priority_lut[transitions];\n    \n    // Create selection masks\n    let is_boil = u32(winner == 3u);\n    let is_melt = u32(winner == 2u);\n    let is_freeze = u32(winner == 1u);\n    let is_condense = u32(winner == 0u);\n    \n    // Select new material\n    let new_mat = (boils_into * is_boil) | \n                  (melts_into * is_melt) | \n                  (freezes_into * is_freeze) | \n                  (condenses_into * is_condense);\n    \n    // Compute thermal change (only for melt/boil)\n    let needs_thermal_cost = is_melt | is_boil;\n    let thermal_cost = (lh_melt * is_melt) | (lh_boil * is_boil);\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_thermal_cost != 0u);\n    \n    // Select phase based on winner\n    let new_phase = (PHASE_GAS * is_boil) |\n                    (PHASE_LIQUID * (is_melt | is_condense)) |\n                    (PHASE_FROZEN * is_freeze);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Evolved: Direct priority indexing with precomputed mask table\n// Uses a 16-entry lookup to map transition bitfield directly to winning transition,\n// eliminating sequential priority checks. Fuses thermal adjustment with material selection.\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield (boil=8, melt=4, freeze=2, condense=1)\n    let transitions = (u32(boils_into != 0u && boil_point != 0u && thermal > boil_point) << 3u) |\n                     (u32(melts_into != 0u && melt_point != 0u && thermal > melt_point) << 2u) |\n                     (u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point) << 1u) |\n                     u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: for each 4-bit transition pattern, store the winning bit position\n    // Priority order: boil(3) > melt(2) > freeze(1) > condense(0)\n    // Index is the transition bitfield, value is the bit position of winner\n    let priority_lut = array<u32, 16>(\n        0u, 0u, 1u, 1u,  // 0000, 0001, 0010, 0011\n        2u, 2u, 2u, 2u,  // 0100, 0101, 0110, 0111\n        3u, 3u, 3u, 3u,  // 1000, 1001, 1010, 1011\n        3u, 3u, 3u, 3u   // 1100, 1101, 1110, 1111\n    );\n    \n    let winner = priority_lut[transitions];\n    \n    // Create selection masks\n    let is_boil = u32(winner == 3u);\n    let is_melt = u32(winner == 2u);\n    let is_freeze = u32(winner == 1u);\n    let is_condense = u32(winner == 0u);\n    \n    // Select new material\n    let new_mat = (boils_into * is_boil) | \n                  (melts_into * is_melt) | \n                  (freezes_into * is_freeze) | \n                  (condenses_into * is_condense);\n    \n    // Compute thermal change (only for melt/boil)\n    let needs_thermal_cost = is_melt | is_boil;\n    let thermal_cost = (lh_melt * is_melt) | (lh_boil * is_boil);\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_thermal_cost != 0u);\n    \n    // Select phase based on winner\n    let new_phase = (PHASE_GAS * is_boil) |\n                    (PHASE_LIQUID * (is_melt | is_condense)) |\n                    (PHASE_FROZEN * is_freeze);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen19_5650",
    "generation": 19,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen18_5615"
    ],
    "mutation_description": "1. Parent 1: Bitwise priority masks with direct computation",
    "code_diff": "1. Parent 1: Bitwise priority masks with direct computation",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.1660750000155531,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:54:10.866129",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield (bit 3=boil, 2=melt, 1=freeze, 0=condense)\n    let transitions = (u32(boils_into != 0u && boil_point != 0u && thermal > boil_point) << 3u) |\n                     (u32(melts_into != 0u && melt_point != 0u && thermal > melt_point) << 2u) |\n                     (u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point) << 1u) |\n                     u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority bit using bit manipulation\n    // Priority: boil(8) > melt(4) > freeze(2) > condense(1)\n    // Use bit isolation trick: x & -x gives lowest set bit, but we want highest\n    // So we'll use a different approach: progressively mask off lower priority bits\n    let has_boil = (transitions & 8u) >> 3u;\n    let has_melt = ((transitions & 4u) >> 2u) * (1u - has_boil);\n    let has_freeze = ((transitions & 2u) >> 1u) * (1u - has_boil) * (1u - has_melt);\n    let has_condense = (transitions & 1u) * (1u - has_boil) * (1u - has_melt) * (1u - has_freeze);\n    \n    // Compute all possible outputs in parallel\n    let mat_boil = boils_into * has_boil;\n    let mat_melt = melts_into * has_melt;\n    let mat_freeze = freezes_into * has_freeze;\n    let mat_condense = condenses_into * has_condense;\n    let new_mat = mat_boil | mat_melt | mat_freeze | mat_condense;\n    \n    // Thermal calculations (only melt/boil consume energy)\n    let thermal_cost = (lh_boil * has_boil) | (lh_melt * has_melt);\n    let safe_thermal = select(thermal, thermal - thermal_cost, thermal >= thermal_cost);\n    let new_thermal = select(thermal, safe_thermal, (has_boil | has_melt) != 0u);\n    \n    // Phase calculations\n    let phase_gas = PHASE_GAS * has_boil;\n    let phase_liquid = PHASE_LIQUID * (has_melt | has_condense);\n    let phase_frozen = PHASE_FROZEN * has_freeze;\n    let new_phase = phase_gas | phase_liquid | phase_frozen;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Variant: Branchless priority encoding with parallel output computation\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield (bit 3=boil, 2=melt, 1=freeze, 0=condense)\n    let transitions = (u32(boils_into != 0u && boil_point != 0u && thermal > boil_point) << 3u) |\n                     (u32(melts_into != 0u && melt_point != 0u && thermal > melt_point) << 2u) |\n                     (u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point) << 1u) |\n                     u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority bit using bit manipulation\n    // Priority: boil(8) > melt(4) > freeze(2) > condense(1)\n    // Use bit isolation trick: x & -x gives lowest set bit, but we want highest\n    // So we'll use a different approach: progressively mask off lower priority bits\n    let has_boil = (transitions & 8u) >> 3u;\n    let has_melt = ((transitions & 4u) >> 2u) * (1u - has_boil);\n    let has_freeze = ((transitions & 2u) >> 1u) * (1u - has_boil) * (1u - has_melt);\n    let has_condense = (transitions & 1u) * (1u - has_boil) * (1u - has_melt) * (1u - has_freeze);\n    \n    // Compute all possible outputs in parallel\n    let mat_boil = boils_into * has_boil;\n    let mat_melt = melts_into * has_melt;\n    let mat_freeze = freezes_into * has_freeze;\n    let mat_condense = condenses_into * has_condense;\n    let new_mat = mat_boil | mat_melt | mat_freeze | mat_condense;\n    \n    // Thermal calculations (only melt/boil consume energy)\n    let thermal_cost = (lh_boil * has_boil) | (lh_melt * has_melt);\n    let safe_thermal = select(thermal, thermal - thermal_cost, thermal >= thermal_cost);\n    let new_thermal = select(thermal, safe_thermal, (has_boil | has_melt) != 0u);\n    \n    // Phase calculations\n    let phase_gas = PHASE_GAS * has_boil;\n    let phase_liquid = PHASE_LIQUID * (has_melt | has_condense);\n    let phase_frozen = PHASE_FROZEN * has_freeze;\n    let new_phase = phase_gas | phase_liquid | phase_frozen;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen20_5689",
    "generation": 20,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen18_5615"
    ],
    "mutation_description": "LLM-generated WGSL variant",
    "code_diff": "LLM-generated WGSL variant",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.21090000227559358,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:54:49.404911",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition thresholds as vec4 (melt, boil, freeze, condense)\n    let thresholds = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Vectorized transition checks\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = vec2<u32>(u32(thermal > thresholds.x), u32(thermal > thresholds.y));\n    let cold_checks = vec2<u32>(u32(thermal < thresholds.z), u32(thermal < thresholds.w));\n    \n    // Build transition bitfield: boil=8, melt=4, freeze=2, condense=1\n    let valid_targets = vec4<u32>(u32(targets.x != 0u), u32(targets.y != 0u), \n                                   u32(targets.z != 0u), u32(targets.w != 0u));\n    let valid_thresholds = vec4<u32>(u32(thresholds.x != 0u), u32(thresholds.y != 0u),\n                                      u32(thresholds.z != 0u), u32(thresholds.w != 0u));\n    \n    let transitions = (valid_targets.y * valid_thresholds.y * hot_checks.y) << 3u |\n                     (valid_targets.x * valid_thresholds.x * hot_checks.x) << 2u |\n                     (valid_targets.z * valid_thresholds.z * cold_checks.x) << 1u |\n                     (valid_targets.w * valid_thresholds.w * cold_checks.y);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Use bit manipulation to find highest priority transition\n    // For 4-bit field, we can use a simple formula instead of clz\n    let priority_bit = select(select(select(1u, 2u, (transitions & 2u) != 0u),\n                                    4u, (transitions & 4u) != 0u),\n                             8u, (transitions & 8u) != 0u);\n    \n    // Decode which transition won (boil=3, melt=2, freeze=1, condense=0)\n    let winner_idx = select(select(select(0u, 1u, priority_bit == 2u),\n                                  2u, priority_bit == 4u),\n                           3u, priority_bit == 8u);\n    \n    // Load latent heat values\n    let lh_melt = ct(mat, 10u);\n    let lh_boil = ct(mat, 11u);\n    \n    // Unified material selection using winner index\n    let material_array = array<u32, 4>(targets.w, targets.z, targets.x, targets.y);\n    let new_mat = material_array[winner_idx];\n    \n    // Thermal cost only for melt(2) and boil(3)\n    let thermal_cost = select(0u, select(lh_melt, lh_boil, winner_idx == 3u), winner_idx >= 2u);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Phase selection using winner index\n    let phase_array = array<u32, 4>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = phase_array[winner_idx];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Phase 2c: Vectorized transition checks with fused material/phase selection\n// Packs transition checks into vec4 ops and uses unified selection formula\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition thresholds as vec4 (melt, boil, freeze, condense)\n    let thresholds = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Vectorized transition checks\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = vec2<u32>(u32(thermal > thresholds.x), u32(thermal > thresholds.y));\n    let cold_checks = vec2<u32>(u32(thermal < thresholds.z), u32(thermal < thresholds.w));\n    \n    // Build transition bitfield: boil=8, melt=4, freeze=2, condense=1\n    let valid_targets = vec4<u32>(u32(targets.x != 0u), u32(targets.y != 0u), \n                                   u32(targets.z != 0u), u32(targets.w != 0u));\n    let valid_thresholds = vec4<u32>(u32(thresholds.x != 0u), u32(thresholds.y != 0u),\n                                      u32(thresholds.z != 0u), u32(thresholds.w != 0u));\n    \n    let transitions = (valid_targets.y * valid_thresholds.y * hot_checks.y) << 3u |\n                     (valid_targets.x * valid_thresholds.x * hot_checks.x) << 2u |\n                     (valid_targets.z * valid_thresholds.z * cold_checks.x) << 1u |\n                     (valid_targets.w * valid_thresholds.w * cold_checks.y);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Use bit manipulation to find highest priority transition\n    // For 4-bit field, we can use a simple formula instead of clz\n    let priority_bit = select(select(select(1u, 2u, (transitions & 2u) != 0u),\n                                    4u, (transitions & 4u) != 0u),\n                             8u, (transitions & 8u) != 0u);\n    \n    // Decode which transition won (boil=3, melt=2, freeze=1, condense=0)\n    let winner_idx = select(select(select(0u, 1u, priority_bit == 2u),\n                                  2u, priority_bit == 4u),\n                           3u, priority_bit == 8u);\n    \n    // Load latent heat values\n    let lh_melt = ct(mat, 10u);\n    let lh_boil = ct(mat, 11u);\n    \n    // Unified material selection using winner index\n    let material_array = array<u32, 4>(targets.w, targets.z, targets.x, targets.y);\n    let new_mat = material_array[winner_idx];\n    \n    // Thermal cost only for melt(2) and boil(3)\n    let thermal_cost = select(0u, select(lh_melt, lh_boil, winner_idx == 3u), winner_idx >= 2u);\n    let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n    \n    // Phase selection using winner index\n    let phase_array = array<u32, 4>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = phase_array[winner_idx];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen21_5724",
    "generation": 21,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen18_5615"
    ],
    "mutation_description": "free arithmetic priority selection** that avoids both LUT overhead and bit manipulation functions, using multiplication ",
    "code_diff": "free arithmetic priority selection** that avoids both LUT overhead and bit manipulation functions, using multiplication ",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.17091999616241083,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:55:24.417524",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Compute transition flags as u32 (0 or 1)\n    let can_boil     = u32(boils_into != 0u && boil_point != 0u && thermal > boil_point);\n    let can_melt     = u32(melts_into != 0u && melt_point != 0u && thermal > melt_point);\n    let can_freeze   = u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point);\n    let can_condense = u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    let any_transition = can_boil | can_melt | can_freeze | can_condense;\n    if (any_transition == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Arithmetic priority selection: multiply each flag by its priority weight\n    // then use max operations to find winner without branches\n    let boil_priority     = can_boil * 4u;\n    let melt_priority     = can_melt * 3u;\n    let freeze_priority   = can_freeze * 2u;\n    let condense_priority = can_condense * 1u;\n    \n    // Find maximum priority value\n    let max_priority = max(max(boil_priority, melt_priority), \n                          max(freeze_priority, condense_priority));\n    \n    // Create selection masks by comparing each priority to max\n    let is_boil     = u32(boil_priority == max_priority);\n    let is_melt     = u32(melt_priority == max_priority) * (1u - is_boil);\n    let is_freeze   = u32(freeze_priority == max_priority) * (1u - is_boil) * (1u - is_melt);\n    let is_condense = u32(condense_priority == max_priority) * (1u - is_boil) * (1u - is_melt) * (1u - is_freeze);\n    \n    // Compute new values using arithmetic selection\n    let new_mat = boils_into * is_boil + \n                  melts_into * is_melt + \n                  freezes_into * is_freeze + \n                  condenses_into * is_condense;\n    \n    // Thermal cost computation with saturating subtraction\n    let thermal_cost = lh_boil * is_boil + lh_melt * is_melt;\n    let new_thermal = select(thermal - thermal_cost, 0u, thermal < thermal_cost);\n    \n    // Phase selection using arithmetic\n    let new_phase = PHASE_GAS * is_boil +\n                    PHASE_LIQUID * (is_melt + is_condense) +\n                    PHASE_FROZEN * is_freeze;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Branch-free arithmetic priority selection with fused operations\n// Uses multiplication chains to compute priority without branches or LUTs\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Compute transition flags as u32 (0 or 1)\n    let can_boil     = u32(boils_into != 0u && boil_point != 0u && thermal > boil_point);\n    let can_melt     = u32(melts_into != 0u && melt_point != 0u && thermal > melt_point);\n    let can_freeze   = u32(freezes_into != 0u && freeze_point != 0u && thermal < freeze_point);\n    let can_condense = u32(condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    let any_transition = can_boil | can_melt | can_freeze | can_condense;\n    if (any_transition == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Arithmetic priority selection: multiply each flag by its priority weight\n    // then use max operations to find winner without branches\n    let boil_priority     = can_boil * 4u;\n    let melt_priority     = can_melt * 3u;\n    let freeze_priority   = can_freeze * 2u;\n    let condense_priority = can_condense * 1u;\n    \n    // Find maximum priority value\n    let max_priority = max(max(boil_priority, melt_priority), \n                          max(freeze_priority, condense_priority));\n    \n    // Create selection masks by comparing each priority to max\n    let is_boil     = u32(boil_priority == max_priority);\n    let is_melt     = u32(melt_priority == max_priority) * (1u - is_boil);\n    let is_freeze   = u32(freeze_priority == max_priority) * (1u - is_boil) * (1u - is_melt);\n    let is_condense = u32(condense_priority == max_priority) * (1u - is_boil) * (1u - is_melt) * (1u - is_freeze);\n    \n    // Compute new values using arithmetic selection\n    let new_mat = boils_into * is_boil + \n                  melts_into * is_melt + \n                  freezes_into * is_freeze + \n                  condenses_into * is_condense;\n    \n    // Thermal cost computation with saturating subtraction\n    let thermal_cost = lh_boil * is_boil + lh_melt * is_melt;\n    let new_thermal = select(thermal - thermal_cost, 0u, thermal < thermal_cost);\n    \n    // Phase selection using arithmetic\n    let new_phase = PHASE_GAS * is_boil +\n                    PHASE_LIQUID * (is_melt + is_condense) +\n                    PHASE_FROZEN * is_freeze;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen22_5763",
    "generation": 22,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen18_5615"
    ],
    "mutation_description": "1. Parent 1: Bitwise priority masks with manual priority computation",
    "code_diff": "1. Parent 1: Bitwise priority masks with manual priority computation",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.14611500373575836,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:56:03.570201",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points and targets as vec4s\n    let points = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));  // melt, boil, freeze, condense\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));  // lh_melt, lh_boil\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));  // melts_into, boils_into, freezes_into, condenses_into\n\n    // Build transition conditions as vec4<bool>\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = thermal_vec > vec4<u32>(points.x, points.y, 0u, 0u);  // melt, boil conditions\n    let cold_checks = thermal_vec < vec4<u32>(255u, 255u, points.z, points.w);  // freeze, condense conditions\n    \n    // Combine conditions with target validity\n    let valid_targets = targets != vec4<u32>(0u);\n    let valid_points = points != vec4<u32>(0u);\n    \n    // Build final transition mask (melt=bit0, boil=bit1, freeze=bit2, condense=bit3)\n    let transitions = u32(valid_targets.x && valid_points.x && hot_checks.x) |\n                     (u32(valid_targets.y && valid_points.y && hot_checks.y) << 1u) |\n                     (u32(valid_targets.z && valid_points.z && cold_checks.z) << 2u) |\n                     (u32(valid_targets.w && valid_points.w && cold_checks.w) << 3u);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority transition (boil > melt > freeze > condense)\n    // Reverse bit order: condense=8, freeze=4, melt=2, boil=1\n    let priority_bits = ((transitions & 1u) << 1u) |    // melt to bit 1\n                       ((transitions & 2u) >> 1u) |     // boil to bit 0 (highest)\n                       ((transitions & 4u) << 0u) |     // freeze to bit 2\n                       ((transitions & 8u) >> 1u);      // condense to bit 3\n    \n    // Get the highest priority bit position\n    let winner_bit = 31u - countLeadingZeros(priority_bits);\n    \n    // Convert back to original indices\n    let is_boil = u32(winner_bit == 0u);\n    let is_melt = u32(winner_bit == 1u);\n    let is_freeze = u32(winner_bit == 2u);\n    let is_condense = u32(winner_bit == 3u);\n    \n    // Select material using dot product\n    let material_selector = vec4<u32>(is_melt, is_boil, is_freeze, is_condense);\n    let new_mat = dot(targets, material_selector);\n    \n    // Compute thermal change\n    let needs_latent = is_melt | is_boil;\n    let thermal_cost = latent.x * is_melt + latent.y * is_boil;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_latent != 0u);\n    \n    // Compute phase\n    let phase_values = vec4<u32>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = dot(phase_values, material_selector);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Unified bitwise priority with vectorized material/phase selection\n// Uses vec4 operations to compute all transition targets in parallel and select winner via bitwise priority\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points and targets as vec4s\n    let points = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));  // melt, boil, freeze, condense\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));  // lh_melt, lh_boil\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));  // melts_into, boils_into, freezes_into, condenses_into\n\n    // Build transition conditions as vec4<bool>\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_checks = thermal_vec > vec4<u32>(points.x, points.y, 0u, 0u);  // melt, boil conditions\n    let cold_checks = thermal_vec < vec4<u32>(255u, 255u, points.z, points.w);  // freeze, condense conditions\n    \n    // Combine conditions with target validity\n    let valid_targets = targets != vec4<u32>(0u);\n    let valid_points = points != vec4<u32>(0u);\n    \n    // Build final transition mask (melt=bit0, boil=bit1, freeze=bit2, condense=bit3)\n    let transitions = u32(valid_targets.x && valid_points.x && hot_checks.x) |\n                     (u32(valid_targets.y && valid_points.y && hot_checks.y) << 1u) |\n                     (u32(valid_targets.z && valid_points.z && cold_checks.z) << 2u) |\n                     (u32(valid_targets.w && valid_points.w && cold_checks.w) << 3u);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority transition (boil > melt > freeze > condense)\n    // Reverse bit order: condense=8, freeze=4, melt=2, boil=1\n    let priority_bits = ((transitions & 1u) << 1u) |    // melt to bit 1\n                       ((transitions & 2u) >> 1u) |     // boil to bit 0 (highest)\n                       ((transitions & 4u) << 0u) |     // freeze to bit 2\n                       ((transitions & 8u) >> 1u);      // condense to bit 3\n    \n    // Get the highest priority bit position\n    let winner_bit = 31u - countLeadingZeros(priority_bits);\n    \n    // Convert back to original indices\n    let is_boil = u32(winner_bit == 0u);\n    let is_melt = u32(winner_bit == 1u);\n    let is_freeze = u32(winner_bit == 2u);\n    let is_condense = u32(winner_bit == 3u);\n    \n    // Select material using dot product\n    let material_selector = vec4<u32>(is_melt, is_boil, is_freeze, is_condense);\n    let new_mat = dot(targets, material_selector);\n    \n    // Compute thermal change\n    let needs_latent = is_melt | is_boil;\n    let thermal_cost = latent.x * is_melt + latent.y * is_boil;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_latent != 0u);\n    \n    // Compute phase\n    let phase_values = vec4<u32>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = dot(phase_values, material_selector);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen23_5796",
    "generation": 23,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen22_5763"
    ],
    "mutation_description": "```wgsl",
    "code_diff": "```wgsl",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.15191500133369118,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:56:36.260339",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters in two batches to reduce register pressure\n    let melt_point = ct(mat, 2u);\n    let boil_point = ct(mat, 3u);\n    let melts_into = ct(mat, 12u);\n    let boils_into = ct(mat, 13u);\n    \n    // Build hot transitions bitfield\n    let hot_transitions = (u32(boils_into != 0u & boil_point != 0u & thermal > boil_point) << 1u) |\n                         u32(melts_into != 0u & melt_point != 0u & thermal > melt_point);\n    \n    // Early exit if hot transitions (common case in many simulations)\n    if (hot_transitions != 0u) {\n        // Use bit manipulation to find highest priority\n        let is_boil = hot_transitions >> 1u;\n        let thermal_cost = ct(mat, 10u + is_boil);  // 10=lh_melt, 11=lh_boil\n        let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n        let new_mat = select(melts_into, boils_into, is_boil != 0u);\n        let new_phase = select(PHASE_LIQUID, PHASE_GAS, is_boil != 0u);\n        \n        grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n        return;\n    }\n    \n    // Check cold transitions only if no hot transitions\n    let freeze_point = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let freezes_into = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n    \n    let cold_transitions = (u32(freezes_into != 0u & freeze_point != 0u & thermal < freeze_point) << 1u) |\n                          u32(condenses_into != 0u & condense_point != 0u & thermal < condense_point);\n    \n    if (cold_transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Handle cold transitions (freeze has priority over condense)\n    let is_freeze = cold_transitions >> 1u;\n    let new_mat = select(condenses_into, freezes_into, is_freeze != 0u);\n    let new_phase = select(PHASE_LIQUID, PHASE_FROZEN, is_freeze != 0u);\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, thermal, new_phase);\n}",
      "_full_shader": "// Hybrid bitfield approach with optimized priority resolution\n// Combines Parent 1's bitwise masks with Parent 2's single-transition fast path using minimal instructions\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition parameters in two batches to reduce register pressure\n    let melt_point = ct(mat, 2u);\n    let boil_point = ct(mat, 3u);\n    let melts_into = ct(mat, 12u);\n    let boils_into = ct(mat, 13u);\n    \n    // Build hot transitions bitfield\n    let hot_transitions = (u32(boils_into != 0u & boil_point != 0u & thermal > boil_point) << 1u) |\n                         u32(melts_into != 0u & melt_point != 0u & thermal > melt_point);\n    \n    // Early exit if hot transitions (common case in many simulations)\n    if (hot_transitions != 0u) {\n        // Use bit manipulation to find highest priority\n        let is_boil = hot_transitions >> 1u;\n        let thermal_cost = ct(mat, 10u + is_boil);  // 10=lh_melt, 11=lh_boil\n        let new_thermal = select(0u, thermal - thermal_cost, thermal >= thermal_cost);\n        let new_mat = select(melts_into, boils_into, is_boil != 0u);\n        let new_phase = select(PHASE_LIQUID, PHASE_GAS, is_boil != 0u);\n        \n        grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n        return;\n    }\n    \n    // Check cold transitions only if no hot transitions\n    let freeze_point = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let freezes_into = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n    \n    let cold_transitions = (u32(freezes_into != 0u & freeze_point != 0u & thermal < freeze_point) << 1u) |\n                          u32(condenses_into != 0u & condense_point != 0u & thermal < condense_point);\n    \n    if (cold_transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Handle cold transitions (freeze has priority over condense)\n    let is_freeze = cold_transitions >> 1u;\n    let new_mat = select(condenses_into, freezes_into, is_freeze != 0u);\n    let new_phase = select(PHASE_LIQUID, PHASE_FROZEN, is_freeze != 0u);\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen24_5833",
    "generation": 24,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen22_5763"
    ],
    "mutation_description": "entry lookup table packed into a 64-bit constant, with extractBits to efficiently select the winning transition.",
    "code_diff": "entry lookup table packed into a 64-bit constant, with extractBits to efficiently select the winning transition.",
    "fitness": {
      "energy_conservation_error": Infinity,
      "matter_conservation_error": Infinity,
      "determinism_score": 0.0,
      "tick_time_ms": Infinity,
      "diverged": true,
      "error": "Validation Error\n\nCaused by:\n  In wgpuDeviceCreateShaderModule\n\nShader '' parsing error: numeric literal not representable by target type: `0x3333222233331110u`\n    \u250c\u2500 wgsl:227:24\n    \u2502\n227 \u2502     let priority_lut = 0x3333222233331110u;  // 64-bit constant packed as 16 4-bit values\n    \u2502                        ^^^^^^^^^^^^^^^^^^^ numeric literal not representable by target type\n\n",
      "disqualified": true,
      "disqualification_reason": "Shader compilation or pipeline creation failed"
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:57:13.223777",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load all transition parameters using extractBits for efficient access\n    let table_base = mat * COLD_STRIDE_L;\n    let points_data = vec4<u32>(\n        cold_table[table_base + 2u],   // melt_point\n        cold_table[table_base + 3u],   // boil_point\n        cold_table[table_base + 8u],   // freeze_point\n        cold_table[table_base + 9u]    // condense_point\n    );\n    \n    let latent_data = vec2<u32>(\n        cold_table[table_base + 10u],  // lh_melt\n        cold_table[table_base + 11u]   // lh_boil\n    );\n    \n    let targets_data = vec4<u32>(\n        cold_table[table_base + 12u],  // melts_into\n        cold_table[table_base + 13u],  // boils_into\n        cold_table[table_base + 14u],  // freezes_into\n        cold_table[table_base + 15u]   // condenses_into\n    );\n\n    // Build transition bitfield (bit 0=condense, 1=freeze, 2=melt, 3=boil)\n    let valid_mask = vec4<u32>(\n        u32(targets_data.w != 0u && points_data.w != 0u),\n        u32(targets_data.z != 0u && points_data.z != 0u),\n        u32(targets_data.x != 0u && points_data.x != 0u),\n        u32(targets_data.y != 0u && points_data.y != 0u)\n    );\n    \n    let temp_mask = vec4<u32>(\n        u32(thermal < points_data.w),  // condense\n        u32(thermal < points_data.z),  // freeze\n        u32(thermal > points_data.x),  // melt\n        u32(thermal > points_data.y)   // boil\n    );\n    \n    let transitions = dot(valid_mask & temp_mask, vec4<u32>(1u, 2u, 4u, 8u));\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: for each 4-bit transition combination,\n    // store which transition wins (0=condense, 1=freeze, 2=melt, 3=boil)\n    // Priority order: boil > melt > freeze > condense\n    let priority_lut = 0x3333222233331110u;  // 64-bit constant packed as 16 4-bit values\n    \n    // Extract the winning transition index from lookup table\n    let winner_idx = extractBits(priority_lut, transitions * 4u, 4u);\n    \n    // Use winner_idx to select the appropriate values\n    let selector = vec4<u32>(\n        u32(winner_idx == 0u),  // condense\n        u32(winner_idx == 1u),  // freeze\n        u32(winner_idx == 2u),  // melt\n        u32(winner_idx == 3u)   // boil\n    );\n    \n    // Compute new material\n    let new_mat = dot(vec4<u32>(targets_data.w, targets_data.z, targets_data.x, targets_data.y), selector);\n    \n    // Compute thermal change (only for melt/boil)\n    let thermal_cost = latent_data.x * selector.z + latent_data.y * selector.w;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), thermal_cost != 0u);\n    \n    // Compute new phase\n    let phase_lut = vec4<u32>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = dot(phase_lut, selector);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Phase 2c: Lookup table-based phase transitions with extractBits optimization\n// Uses a 16-entry lookup table indexed by transition bitfield to eliminate all priority branching\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load all transition parameters using extractBits for efficient access\n    let table_base = mat * COLD_STRIDE_L;\n    let points_data = vec4<u32>(\n        cold_table[table_base + 2u],   // melt_point\n        cold_table[table_base + 3u],   // boil_point\n        cold_table[table_base + 8u],   // freeze_point\n        cold_table[table_base + 9u]    // condense_point\n    );\n    \n    let latent_data = vec2<u32>(\n        cold_table[table_base + 10u],  // lh_melt\n        cold_table[table_base + 11u]   // lh_boil\n    );\n    \n    let targets_data = vec4<u32>(\n        cold_table[table_base + 12u],  // melts_into\n        cold_table[table_base + 13u],  // boils_into\n        cold_table[table_base + 14u],  // freezes_into\n        cold_table[table_base + 15u]   // condenses_into\n    );\n\n    // Build transition bitfield (bit 0=condense, 1=freeze, 2=melt, 3=boil)\n    let valid_mask = vec4<u32>(\n        u32(targets_data.w != 0u && points_data.w != 0u),\n        u32(targets_data.z != 0u && points_data.z != 0u),\n        u32(targets_data.x != 0u && points_data.x != 0u),\n        u32(targets_data.y != 0u && points_data.y != 0u)\n    );\n    \n    let temp_mask = vec4<u32>(\n        u32(thermal < points_data.w),  // condense\n        u32(thermal < points_data.z),  // freeze\n        u32(thermal > points_data.x),  // melt\n        u32(thermal > points_data.y)   // boil\n    );\n    \n    let transitions = dot(valid_mask & temp_mask, vec4<u32>(1u, 2u, 4u, 8u));\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority lookup table: for each 4-bit transition combination,\n    // store which transition wins (0=condense, 1=freeze, 2=melt, 3=boil)\n    // Priority order: boil > melt > freeze > condense\n    let priority_lut = 0x3333222233331110u;  // 64-bit constant packed as 16 4-bit values\n    \n    // Extract the winning transition index from lookup table\n    let winner_idx = extractBits(priority_lut, transitions * 4u, 4u);\n    \n    // Use winner_idx to select the appropriate values\n    let selector = vec4<u32>(\n        u32(winner_idx == 0u),  // condense\n        u32(winner_idx == 1u),  // freeze\n        u32(winner_idx == 2u),  // melt\n        u32(winner_idx == 3u)   // boil\n    );\n    \n    // Compute new material\n    let new_mat = dot(vec4<u32>(targets_data.w, targets_data.z, targets_data.x, targets_data.y), selector);\n    \n    // Compute thermal change (only for melt/boil)\n    let thermal_cost = latent_data.x * selector.z + latent_data.y * selector.w;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), thermal_cost != 0u);\n    \n    // Compute new phase\n    let phase_lut = vec4<u32>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = dot(phase_lut, selector);\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen25_5871",
    "generation": 25,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen22_5763"
    ],
    "mutation_description": "entry lookup table encoded in a 64-bit constant to instantly resolve priority conflicts between multiple transitions.",
    "code_diff": "entry lookup table encoded in a 64-bit constant to instantly resolve priority conflicts between multiple transitions.",
    "fitness": {
      "energy_conservation_error": Infinity,
      "matter_conservation_error": Infinity,
      "determinism_score": 0.0,
      "tick_time_ms": Infinity,
      "diverged": true,
      "error": "Validation Error\n\nCaused by:\n  In wgpuDeviceCreateShaderModule\n\nShader '' parsing error: numeric literal not representable by target type: `0x3333222233331110u`\n    \u250c\u2500 wgsl:208:24\n    \u2502\n208 \u2502     let priority_lut = 0x3333222233331110u;  // 64-bit constant encoding 16 2-bit values\n    \u2502                        ^^^^^^^^^^^^^^^^^^^ numeric literal not representable by target type\n\n",
      "disqualified": true,
      "disqualification_reason": "Shader compilation or pipeline creation failed"
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:57:51.728076",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Vectorized cold table loading\n    let points_melt_boil = vec2<u32>(ct(mat, 2u), ct(mat, 3u));\n    let points_freeze_condense = vec2<u32>(ct(mat, 8u), ct(mat, 9u));\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));\n    let targets_melt_boil = vec2<u32>(ct(mat, 12u), ct(mat, 13u));\n    let targets_freeze_condense = vec2<u32>(ct(mat, 14u), ct(mat, 15u));\n\n    // Build transition mask using vector comparisons\n    let thermal_v = vec2<u32>(thermal);\n    let hot_valid = (targets_melt_boil != vec2<u32>(0u)) & (points_melt_boil != vec2<u32>(0u));\n    let cold_valid = (targets_freeze_condense != vec2<u32>(0u)) & (points_freeze_condense != vec2<u32>(0u));\n    \n    let hot_active = hot_valid & (thermal_v > points_melt_boil);\n    let cold_active = cold_valid & (thermal_v < points_freeze_condense);\n    \n    // Pack into 4-bit transition mask (boil=8, melt=4, freeze=2, condense=1)\n    let transitions = (u32(hot_active.y) << 3u) | (u32(hot_active.x) << 2u) |\n                     (u32(cold_active.x) << 1u) | u32(cold_active.y);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority resolution using precomputed LUT\n    // For each 4-bit transition combination, stores which bit wins (0-3)\n    // Priority: boil(3) > melt(2) > freeze(1) > condense(0)\n    let priority_lut = 0x3333222233331110u;  // 64-bit constant encoding 16 2-bit values\n    let winner = (priority_lut >> ((transitions & 15u) << 2u)) & 3u;\n    \n    // Unpack winner back to individual flags\n    let is_condense = u32(winner == 0u);\n    let is_freeze = u32(winner == 1u);\n    let is_melt = u32(winner == 2u);\n    let is_boil = u32(winner == 3u);\n    \n    // Compute new material using masked selection\n    let mat_select = vec4<u32>(is_melt, is_boil, is_freeze, is_condense);\n    let new_mat = targets_melt_boil.x * mat_select.x +\n                  targets_melt_boil.y * mat_select.y +\n                  targets_freeze_condense.x * mat_select.z +\n                  targets_freeze_condense.y * mat_select.w;\n    \n    // Thermal adjustment for endothermic transitions\n    let thermal_cost = latent.x * is_melt + latent.y * is_boil;\n    let has_cost = thermal_cost > 0u;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), has_cost);\n    \n    // Phase determination\n    let phase_gas = PHASE_GAS * is_boil;\n    let phase_liquid = PHASE_LIQUID * (is_melt | is_condense);\n    let phase_frozen = PHASE_FROZEN * is_freeze;\n    let new_phase = phase_gas | phase_liquid | phase_frozen;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Hybrid approach: LUT-based priority resolution with vectorized transition evaluation\n// Uses a precomputed 16-entry priority table to resolve multi-transition cases instantly\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Vectorized cold table loading\n    let points_melt_boil = vec2<u32>(ct(mat, 2u), ct(mat, 3u));\n    let points_freeze_condense = vec2<u32>(ct(mat, 8u), ct(mat, 9u));\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));\n    let targets_melt_boil = vec2<u32>(ct(mat, 12u), ct(mat, 13u));\n    let targets_freeze_condense = vec2<u32>(ct(mat, 14u), ct(mat, 15u));\n\n    // Build transition mask using vector comparisons\n    let thermal_v = vec2<u32>(thermal);\n    let hot_valid = (targets_melt_boil != vec2<u32>(0u)) & (points_melt_boil != vec2<u32>(0u));\n    let cold_valid = (targets_freeze_condense != vec2<u32>(0u)) & (points_freeze_condense != vec2<u32>(0u));\n    \n    let hot_active = hot_valid & (thermal_v > points_melt_boil);\n    let cold_active = cold_valid & (thermal_v < points_freeze_condense);\n    \n    // Pack into 4-bit transition mask (boil=8, melt=4, freeze=2, condense=1)\n    let transitions = (u32(hot_active.y) << 3u) | (u32(hot_active.x) << 2u) |\n                     (u32(cold_active.x) << 1u) | u32(cold_active.y);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Priority resolution using precomputed LUT\n    // For each 4-bit transition combination, stores which bit wins (0-3)\n    // Priority: boil(3) > melt(2) > freeze(1) > condense(0)\n    let priority_lut = 0x3333222233331110u;  // 64-bit constant encoding 16 2-bit values\n    let winner = (priority_lut >> ((transitions & 15u) << 2u)) & 3u;\n    \n    // Unpack winner back to individual flags\n    let is_condense = u32(winner == 0u);\n    let is_freeze = u32(winner == 1u);\n    let is_melt = u32(winner == 2u);\n    let is_boil = u32(winner == 3u);\n    \n    // Compute new material using masked selection\n    let mat_select = vec4<u32>(is_melt, is_boil, is_freeze, is_condense);\n    let new_mat = targets_melt_boil.x * mat_select.x +\n                  targets_melt_boil.y * mat_select.y +\n                  targets_freeze_condense.x * mat_select.z +\n                  targets_freeze_condense.y * mat_select.w;\n    \n    // Thermal adjustment for endothermic transitions\n    let thermal_cost = latent.x * is_melt + latent.y * is_boil;\n    let has_cost = thermal_cost > 0u;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), has_cost);\n    \n    // Phase determination\n    let phase_gas = PHASE_GAS * is_boil;\n    let phase_liquid = PHASE_LIQUID * (is_melt | is_condense);\n    let phase_frozen = PHASE_FROZEN * is_freeze;\n    let new_phase = phase_gas | phase_liquid | phase_frozen;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen26_5906",
    "generation": 26,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen22_5763"
    ],
    "mutation_description": "Direct array indexing with priority-encoded bitfield for minimal branching and register pressure.",
    "code_diff": "Direct array indexing with priority-encoded bitfield for minimal branching and register pressure.",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.1440100051695481,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:58:26.931859",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load all transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield with priority pre-encoded\n    // Use bits 0-3 for priority order: boil=3, melt=2, freeze=1, condense=0\n    let boil_mask = select(0u, 8u, boils_into != 0u && boil_point != 0u && thermal > boil_point);\n    let melt_mask = select(0u, 4u, melts_into != 0u && melt_point != 0u && thermal > melt_point);\n    let freeze_mask = select(0u, 2u, freezes_into != 0u && freeze_point != 0u && thermal < freeze_point);\n    let condense_mask = select(0u, 1u, condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    let transitions = boil_mask | melt_mask | freeze_mask | condense_mask;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest set bit (highest priority transition)\n    let winner = 31u - countLeadingZeros(transitions);\n    \n    // Direct indexing into material array using winner bit\n    let material_array = array<u32, 4>(condenses_into, freezes_into, melts_into, boils_into);\n    let new_mat = material_array[winner];\n    \n    // Compute thermal change (only for melt/boil which are bits 2-3)\n    let needs_latent = winner >> 1u;  // 1 if melt or boil\n    let latent_array = array<u32, 2>(lh_melt, lh_boil);\n    let thermal_cost = latent_array[winner - 2u] * needs_latent;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_latent != 0u);\n    \n    // Compute phase using lookup\n    let phase_array = array<u32, 4>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = phase_array[winner];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Hybrid approach: bitfield construction with simplified priority resolution and fast paths\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load all transition parameters\n    let melt_point     = ct(mat, 2u);\n    let boil_point     = ct(mat, 3u);\n    let freeze_point   = ct(mat, 8u);\n    let condense_point = ct(mat, 9u);\n    let lh_melt        = ct(mat, 10u);\n    let lh_boil        = ct(mat, 11u);\n    let melts_into     = ct(mat, 12u);\n    let boils_into     = ct(mat, 13u);\n    let freezes_into   = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n\n    // Build transition bitfield with priority pre-encoded\n    // Use bits 0-3 for priority order: boil=3, melt=2, freeze=1, condense=0\n    let boil_mask = select(0u, 8u, boils_into != 0u && boil_point != 0u && thermal > boil_point);\n    let melt_mask = select(0u, 4u, melts_into != 0u && melt_point != 0u && thermal > melt_point);\n    let freeze_mask = select(0u, 2u, freezes_into != 0u && freeze_point != 0u && thermal < freeze_point);\n    let condense_mask = select(0u, 1u, condenses_into != 0u && condense_point != 0u && thermal < condense_point);\n    \n    let transitions = boil_mask | melt_mask | freeze_mask | condense_mask;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest set bit (highest priority transition)\n    let winner = 31u - countLeadingZeros(transitions);\n    \n    // Direct indexing into material array using winner bit\n    let material_array = array<u32, 4>(condenses_into, freezes_into, melts_into, boils_into);\n    let new_mat = material_array[winner];\n    \n    // Compute thermal change (only for melt/boil which are bits 2-3)\n    let needs_latent = winner >> 1u;  // 1 if melt or boil\n    let latent_array = array<u32, 2>(lh_melt, lh_boil);\n    let thermal_cost = latent_array[winner - 2u] * needs_latent;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_latent != 0u);\n    \n    // Compute phase using lookup\n    let phase_array = array<u32, 4>(PHASE_LIQUID, PHASE_FROZEN, PHASE_LIQUID, PHASE_GAS);\n    let new_phase = phase_array[winner];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen27_5948",
    "generation": 27,
    "parent_ids": [
      "gen12_5395",
      "gen14_5464",
      "gen26_5906"
    ],
    "mutation_description": "```wgsl",
    "code_diff": "```wgsl",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.15940000012051314,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:59:08.791653",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points and latent heats in two vec4 operations\n    let base_offset = mat * COLD_STRIDE_L;\n    let points_vec = vec4<u32>(\n        cold_table[base_offset + 2u],   // melt_point\n        cold_table[base_offset + 3u],   // boil_point\n        cold_table[base_offset + 8u],   // freeze_point\n        cold_table[base_offset + 9u]    // condense_point\n    );\n    \n    let latent_vec = vec4<u32>(\n        cold_table[base_offset + 10u],  // lh_melt\n        cold_table[base_offset + 11u],  // lh_boil\n        0u,                             // unused\n        0u                              // unused\n    );\n    \n    // Load target materials\n    let melts_into = ct(mat, 12u);\n    let boils_into = ct(mat, 13u);\n    let freezes_into = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n    \n    // Build transition bitfield using vectorized comparisons\n    let hot_checks = vec2<u32>(\n        u32(melts_into != 0u && points_vec.x != 0u && thermal > points_vec.x),\n        u32(boils_into != 0u && points_vec.y != 0u && thermal > points_vec.y)\n    );\n    \n    let cold_checks = vec2<u32>(\n        u32(freezes_into != 0u && points_vec.z != 0u && thermal < points_vec.z),\n        u32(condenses_into != 0u && points_vec.w != 0u && thermal < points_vec.w)\n    );\n    \n    // Priority encoding: boil=8, melt=4, freeze=2, condense=1\n    let transitions = (hot_checks.y << 3u) | (hot_checks.x << 2u) | \n                     (cold_checks.x << 1u) | cold_checks.y;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Find highest priority transition using bit scan\n    let priority_bit = 1u << (31u - countLeadingZeros(transitions));\n    \n    // Decode which transition won (0=condense, 1=freeze, 2=melt, 3=boil)\n    let winner_idx = countTrailingZeros(priority_bit);\n    \n    // Select new material using bit manipulation\n    let mat_selector = vec4<u32>(condenses_into, freezes_into, melts_into, boils_into);\n    let new_mat = mat_selector[winner_idx];\n    \n    // Compute thermal cost (only for hot transitions)\n    let is_hot = winner_idx >> 1u;  // 1 if melt or boil\n    let latent_cost = select(latent_vec.x, latent_vec.y, winner_idx == 3u) * is_hot;\n    let new_thermal = select(thermal, select(0u, thermal - latent_cost, thermal >= latent_cost), is_hot != 0u);\n    \n    // Compute new phase using compact encoding\n    // condense/melt -> liquid (5), freeze -> frozen (1), boil -> gas (4)\n    let phase_bits = 0x4515u;  // encodes [5,1,5,4] in 4-bit chunks\n    let new_phase = (phase_bits >> (winner_idx << 2u)) & 0xFu;\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Hybrid bit manipulation with precomputed transition data\n// Combines Parent 1's bitfield approach with Parent 3's direct indexing,\n// using a single vec4 load to minimize cold table accesses\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition points and latent heats in two vec4 operations\n    let base_offset = mat * COLD_STRIDE_L;\n    let points_vec = vec4<u32>(\n        cold_table[base_offset + 2u],   // melt_point\n        cold_table[base_offset + 3u],   // boil_point\n        cold_table[base_offset + 8u],   // freeze_point\n        cold_table[base_offset + 9u]    // condense_point\n    );\n    \n    let latent_vec = vec4<u32>(\n        cold_table[base_offset + 10u],  // lh_melt\n        cold_table[base_offset + 11u],  // lh_boil\n        0u,                             // unused\n        0u                              // unused\n    );\n    \n    // Load target materials\n    let melts_into = ct(mat, 12u);\n    let boils_into = ct(mat, 13u);\n    let freezes_into = ct(mat, 14u);\n    let condenses_into = ct(mat, 15u);\n    \n    // Build transition bitfield using vectorized comparisons\n    let hot_checks = vec2<u32>(\n        u32(melts_into != 0u && points_vec.x != 0u && thermal > points_vec.x),\n        u32(boils_into != 0u && points_vec.y != 0u && thermal > points_vec.y)\n    );\n    \n    let cold_checks = vec2<u32>(\n        u32(freezes_into != 0u && points_vec.z != 0u && thermal < points_vec.z),\n        u32(condenses_into != 0u && points_vec.w != 0u && thermal < points_vec.w)\n    );\n    \n    // Priority encoding: boil=8, melt=4, freeze=2, condense=1\n    let transitions = (hot_checks.y << 3u) | (hot_checks.x << 2u) | \n                     (cold_checks.x << 1u) | cold_checks.y;\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Find highest priority transition using bit scan\n    let priority_bit = 1u << (31u - countLeadingZeros(transitions));\n    \n    // Decode which transition won (0=condense, 1=freeze, 2=melt, 3=boil)\n    let winner_idx = countTrailingZeros(priority_bit);\n    \n    // Select new material using bit manipulation\n    let mat_selector = vec4<u32>(condenses_into, freezes_into, melts_into, boils_into);\n    let new_mat = mat_selector[winner_idx];\n    \n    // Compute thermal cost (only for hot transitions)\n    let is_hot = winner_idx >> 1u;  // 1 if melt or boil\n    let latent_cost = select(latent_vec.x, latent_vec.y, winner_idx == 3u) * is_hot;\n    let new_thermal = select(thermal, select(0u, thermal - latent_cost, thermal >= latent_cost), is_hot != 0u);\n    \n    // Compute new phase using compact encoding\n    // condense/melt -> liquid (5), freeze -> frozen (1), boil -> gas (4)\n    let phase_bits = 0x4515u;  // encodes [5,1,5,4] in 4-bit chunks\n    let new_phase = (phase_bits >> (winner_idx << 2u)) & 0xFu;\n    \n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen28_5985",
    "generation": 28,
    "parent_ids": [
      "gen14_5464",
      "gen26_5906",
      "gen22_5763"
    ],
    "mutation_description": "pass transition computation with vectorized operations and a compact priority resolution system.",
    "code_diff": "pass transition computation with vectorized operations and a compact priority resolution system.",
    "fitness": {
      "energy_conservation_error": 0.0,
      "matter_conservation_error": 0.0,
      "determinism_score": 1.0,
      "tick_time_ms": 0.33313500171061605,
      "diverged": false
    },
    "phase": "2c",
    "created_at": "2026-04-03T23:59:45.145853",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition data as vec4s for SIMD processing\n    let points_hot = vec2<u32>(ct(mat, 2u), ct(mat, 3u));   // melt, boil points\n    let points_cold = vec2<u32>(ct(mat, 8u), ct(mat, 9u));  // freeze, condense points\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));     // lh_melt, lh_boil\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u)); // melts, boils, freezes, condenses into\n\n    // Vectorized condition evaluation\n    let thermal_v = vec2<u32>(thermal);\n    let hot_valid = (points_hot != vec2<u32>(0u)) & (thermal_v > points_hot);\n    let cold_valid = (points_cold != vec2<u32>(0u)) & (thermal_v < points_cold);\n    \n    // Pack conditions into priority mask (boil=8, melt=4, freeze=2, condense=1)\n    let mask = (u32(targets.x != 0u && hot_valid.x) << 2u) |   // melt -> bit 2\n               (u32(targets.y != 0u && hot_valid.y) << 3u) |   // boil -> bit 3\n               (u32(targets.z != 0u && cold_valid.x) << 1u) |  // freeze -> bit 1\n               (u32(targets.w != 0u && cold_valid.y) << 0u);   // condense -> bit 0\n    \n    if (mask == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority bit (boil > melt > freeze > condense)\n    let winner = firstTrailingBit(mask);\n    \n    // Compute transition using bit manipulation\n    let target_shift = winner << 3u;  // winner * 8 bits per field\n    let new_mat = (targets.x >> target_shift) & 0xFFu;\n    \n    // Thermal adjustment: only bits 2-3 (melt/boil) need latent heat\n    let needs_thermal = winner >> 1u;  // 1 if winner >= 2\n    let latent_idx = winner - 2u;\n    let thermal_cost = ((latent.x >> (latent_idx << 3u)) & 0xFFu) * needs_thermal;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_thermal != 0u);\n    \n    // Phase lookup using bit manipulation\n    // condense=0 -> LIQUID, freeze=1 -> FROZEN, melt=2 -> LIQUID, boil=3 -> GAS\n    let phase_bits = 0x32120000u;  // Packed phases: [GAS, LIQUID, FROZEN, LIQUID]\n    let new_phase = (phase_bits >> (winner << 3u)) & 0xFu;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Vectorized transition logic with packed priority resolution\n// Uses vec4 operations throughout and resolves priority via a single firstTrailingBit() call on a packed mask\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Load transition data as vec4s for SIMD processing\n    let points_hot = vec2<u32>(ct(mat, 2u), ct(mat, 3u));   // melt, boil points\n    let points_cold = vec2<u32>(ct(mat, 8u), ct(mat, 9u));  // freeze, condense points\n    let latent = vec2<u32>(ct(mat, 10u), ct(mat, 11u));     // lh_melt, lh_boil\n    let targets = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u)); // melts, boils, freezes, condenses into\n\n    // Vectorized condition evaluation\n    let thermal_v = vec2<u32>(thermal);\n    let hot_valid = (points_hot != vec2<u32>(0u)) & (thermal_v > points_hot);\n    let cold_valid = (points_cold != vec2<u32>(0u)) & (thermal_v < points_cold);\n    \n    // Pack conditions into priority mask (boil=8, melt=4, freeze=2, condense=1)\n    let mask = (u32(targets.x != 0u && hot_valid.x) << 2u) |   // melt -> bit 2\n               (u32(targets.y != 0u && hot_valid.y) << 3u) |   // boil -> bit 3\n               (u32(targets.z != 0u && cold_valid.x) << 1u) |  // freeze -> bit 1\n               (u32(targets.w != 0u && cold_valid.y) << 0u);   // condense -> bit 0\n    \n    if (mask == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Find highest priority bit (boil > melt > freeze > condense)\n    let winner = firstTrailingBit(mask);\n    \n    // Compute transition using bit manipulation\n    let target_shift = winner << 3u;  // winner * 8 bits per field\n    let new_mat = (targets.x >> target_shift) & 0xFFu;\n    \n    // Thermal adjustment: only bits 2-3 (melt/boil) need latent heat\n    let needs_thermal = winner >> 1u;  // 1 if winner >= 2\n    let latent_idx = winner - 2u;\n    let thermal_cost = ((latent.x >> (latent_idx << 3u)) & 0xFFu) * needs_thermal;\n    let new_thermal = select(thermal, select(0u, thermal - thermal_cost, thermal >= thermal_cost), needs_thermal != 0u);\n    \n    // Phase lookup using bit manipulation\n    // condense=0 -> LIQUID, freeze=1 -> FROZEN, melt=2 -> LIQUID, boil=3 -> GAS\n    let phase_bits = 0x32120000u;  // Packed phases: [GAS, LIQUID, FROZEN, LIQUID]\n    let new_phase = (phase_bits >> (winner << 3u)) & 0xFu;\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  },
  {
    "id": "gen29_6020",
    "generation": 29,
    "parent_ids": [
      "gen14_5464",
      "gen26_5906",
      "gen22_5763"
    ],
    "mutation_description": "```wgsl",
    "code_diff": "```wgsl",
    "fitness": {
      "energy_conservation_error": Infinity,
      "matter_conservation_error": Infinity,
      "determinism_score": 0.0,
      "tick_time_ms": Infinity,
      "diverged": true,
      "error": "Validation Error\n\nCaused by:\n  In wgpuDeviceCreateShaderModule\n\nShader validation error: Entry point tick at Compute is invalid\n    \u250c\u2500 :189:22\n    \u2502\n189 \u2502     let hot_valid = (thermal_vec > points_lo.xy) & (points_lo.xy != vec4<u32>(0u).xy) & (targets_lo.xy != vec4<u32>(0u).xy);\n    \u2502                      ^^^^^^^^^^^^^^^^^^^^^^^^^^ naga::ir::Expression [48]\n    \u2502\n    = Expression [48] is invalid\n    = Operation Greater can't work with [46] (of type Vector { size: Quad, scalar: Scalar { kind: Uint, width: 4 } }) and [47] (of type Vector { size: Bi, scalar: Scalar { kind: Uint, width: 4 } })\n\n\n      Expression [48] is invalid\n        Operation Greater can't work with [46] (of type Vector { size: Quad, scalar: Scalar { kind: Uint, width: 4 } }) and [47] (of type Vector { size: Bi, scalar: Scalar { kind: Uint, width: 4 } })",
      "disqualified": true,
      "disqualification_reason": "Shader compilation or pipeline creation failed"
    },
    "phase": "2c",
    "created_at": "2026-04-04T00:00:20.308051",
    "evolvable_code": {
      "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Pack cold table lookups into vec4 operations\n    let points_lo = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let points_hi = vec4<u32>(ct(mat, 10u), ct(mat, 11u), 0u, 0u);\n    let targets_lo = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Build transition mask using vec4 comparisons\n    // bits: 0=melt, 1=boil, 2=freeze, 3=condense\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_valid = (thermal_vec > points_lo.xy) & (points_lo.xy != vec4<u32>(0u).xy) & (targets_lo.xy != vec4<u32>(0u).xy);\n    let cold_valid = (thermal_vec < points_lo.zw) & (points_lo.zw != vec4<u32>(0u).zw) & (targets_lo.zw != vec4<u32>(0u).zw);\n    \n    let transitions = u32(hot_valid.x) | (u32(hot_valid.y) << 1u) | \n                     (u32(cold_valid.x) << 2u) | (u32(cold_valid.y) << 3u);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Priority encoding: boil(1) > melt(0) > freeze(2) > condense(3)\n    // Remap bits to priority order: 0123 -> 1023\n    let priority_map = (transitions & 2u) >> 1u |  // boil to bit 0\n                      (transitions & 1u) << 1u |    // melt to bit 1  \n                      (transitions & 12u);          // freeze/condense stay\n    \n    // Find highest priority (lowest bit)\n    let winner = countTrailingZeros(priority_map);\n    \n    // Unmap winner back to original index\n    let idx_map = array<u32, 4>(1u, 0u, 2u, 3u);  // priority position to transition index\n    let trans_idx = idx_map[winner];\n    \n    // Extract winning values using bit manipulation\n    let new_mat = (targets_lo[trans_idx / 1u] >> ((trans_idx % 1u) * 0u)) & 0xFFu;\n    \n    // Compute thermal change (only for indices 0,1 = melt,boil)\n    let needs_latent = trans_idx < 2u;\n    let latent_cost = select(0u, points_hi[trans_idx], needs_latent);\n    let new_thermal = select(thermal, select(0u, thermal - latent_cost, thermal >= latent_cost), needs_latent);\n    \n    // Map transition index to phase\n    let phase_map = array<u32, 4>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = phase_map[trans_idx];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}",
      "_full_shader": "// Fused transition evaluation with direct bit-to-index mapping\n// Uses a single packed transition state to minimize registers and branches\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE_L: u32 = 16u;\n\nfn ct(mat: u32, field: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE_L + field];\n}\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_thermal(p: u32) -> u32 { return (p >> THERMAL_SHIFT) & THERMAL_MASK; }\n\nfn rewrite_voxel(packed: u32, new_mat: u32, new_thermal: u32, new_phase: u32) -> u32 {\n    let keep = packed & 0xF0FF0000u;\n    return (new_mat & MATERIAL_MASK)\n         | ((new_thermal & THERMAL_MASK) << THERMAL_SHIFT)\n         | keep\n         | ((new_phase & PHASE_MASK) << PHASE_SHIFT);\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let thermal = get_thermal(voxel);\n\n    if (mat == MAT_AIR || mat == MAT_FIRE) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    // Pack cold table lookups into vec4 operations\n    let points_lo = vec4<u32>(ct(mat, 2u), ct(mat, 3u), ct(mat, 8u), ct(mat, 9u));\n    let points_hi = vec4<u32>(ct(mat, 10u), ct(mat, 11u), 0u, 0u);\n    let targets_lo = vec4<u32>(ct(mat, 12u), ct(mat, 13u), ct(mat, 14u), ct(mat, 15u));\n    \n    // Build transition mask using vec4 comparisons\n    // bits: 0=melt, 1=boil, 2=freeze, 3=condense\n    let thermal_vec = vec4<u32>(thermal);\n    let hot_valid = (thermal_vec > points_lo.xy) & (points_lo.xy != vec4<u32>(0u).xy) & (targets_lo.xy != vec4<u32>(0u).xy);\n    let cold_valid = (thermal_vec < points_lo.zw) & (points_lo.zw != vec4<u32>(0u).zw) & (targets_lo.zw != vec4<u32>(0u).zw);\n    \n    let transitions = u32(hot_valid.x) | (u32(hot_valid.y) << 1u) | \n                     (u32(cold_valid.x) << 2u) | (u32(cold_valid.y) << 3u);\n    \n    if (transitions == 0u) {\n        grid_out[idx] = voxel;\n        return;\n    }\n    \n    // Priority encoding: boil(1) > melt(0) > freeze(2) > condense(3)\n    // Remap bits to priority order: 0123 -> 1023\n    let priority_map = (transitions & 2u) >> 1u |  // boil to bit 0\n                      (transitions & 1u) << 1u |    // melt to bit 1  \n                      (transitions & 12u);          // freeze/condense stay\n    \n    // Find highest priority (lowest bit)\n    let winner = countTrailingZeros(priority_map);\n    \n    // Unmap winner back to original index\n    let idx_map = array<u32, 4>(1u, 0u, 2u, 3u);  // priority position to transition index\n    let trans_idx = idx_map[winner];\n    \n    // Extract winning values using bit manipulation\n    let new_mat = (targets_lo[trans_idx / 1u] >> ((trans_idx % 1u) * 0u)) & 0xFFu;\n    \n    // Compute thermal change (only for indices 0,1 = melt,boil)\n    let needs_latent = trans_idx < 2u;\n    let latent_cost = select(0u, points_hi[trans_idx], needs_latent);\n    let new_thermal = select(thermal, select(0u, thermal - latent_cost, thermal >= latent_cost), needs_latent);\n    \n    // Map transition index to phase\n    let phase_map = array<u32, 4>(PHASE_LIQUID, PHASE_GAS, PHASE_FROZEN, PHASE_LIQUID);\n    let new_phase = phase_map[trans_idx];\n\n    grid_out[idx] = rewrite_voxel(voxel, new_mat, new_thermal, new_phase);\n}"
    }
  }
]