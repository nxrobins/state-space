{
  "id": "gen22_9605",
  "generation": 22,
  "parent_ids": [
    "gen18_9457",
    "gen8_9097",
    "gen13_9277"
  ],
  "mutation_description": "LLM-generated WGSL variant",
  "code_diff": "LLM-generated WGSL variant",
  "fitness": {
    "energy_conservation_error": 0.005027684966092357,
    "matter_conservation_error": 0.0,
    "determinism_score": 1.0,
    "tick_time_ms": 0.09620000491850078,
    "diverged": false
  },
  "phase": "2b",
  "created_at": "2026-04-03T22:13:25.680379",
  "evolvable_code": {
    "tick": "fn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let phase = get_phase(voxel);\n\n    if (!is_movable(phase) && mat != MAT_AIR) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    let my_density = density_of(mat);\n    grid_out[idx] = resolve_swap(x, y, voxel, phase, my_density);\n}",
    "_full_shader": "// Optimized gravity with density caching and streamlined swap resolution\n\n@group(0) @binding(0) var<storage, read> grid_in: array<u32>;\n@group(0) @binding(1) var<storage, read_write> grid_out: array<u32>;\n@group(0) @binding(2) var<storage, read> cold_table: array<u32>;\n\nconst COLD_STRIDE: u32 = 8u;\nconst COLD_DENSITY: u32 = 0u;\n\nfn get_idx(x: u32, y: u32) -> u32 { return y * GRID_WIDTH + x; }\nfn get_material(p: u32) -> u32 { return (p >> MATERIAL_SHIFT) & MATERIAL_MASK; }\nfn get_phase(p: u32) -> u32 { return (p >> PHASE_SHIFT) & PHASE_MASK; }\n\nfn density_of(mat: u32) -> u32 {\n    return cold_table[mat * COLD_STRIDE + COLD_DENSITY];\n}\n\nfn is_movable(ph: u32) -> bool {\n    return ph == PHASE_POWDER || ph == PHASE_LIQUID || ph == PHASE_VISCOUS || ph == PHASE_GAS;\n}\n\nfn is_fluid(ph: u32) -> bool {\n    return ph == PHASE_LIQUID || ph == PHASE_VISCOUS || ph == PHASE_GAS;\n}\n\n// Streamlined displacement check\nfn check_displace(nx: u32, ny: u32, my_density: u32) -> u32 {\n    if (nx >= GRID_WIDTH || ny >= GRID_HEIGHT) { return 0u; }\n    let other = grid_in[get_idx(nx, ny)];\n    let other_mat = get_material(other);\n    let other_phase = get_phase(other);\n    let other_density = density_of(other_mat);\n    // Return the voxel if we can displace it, 0 otherwise\n    return select(0u, other, my_density > other_density && (is_movable(other_phase) || other_mat == MAT_AIR));\n}\n\n// Optimized swap resolution with density passed in\nfn resolve_swap(x: u32, y: u32, voxel: u32, phase: u32, my_density: u32) -> u32 {\n    let parity = (x + y) & 1u;\n    \n    // === FALLING (check below) ===\n    if (y < GRID_HEIGHT - 1u) {\n        let below_idx = get_idx(x, y + 1u);\n        let below = grid_in[below_idx];\n        let below_mat = get_material(below);\n        let below_density = density_of(below_mat);\n        let below_phase = get_phase(below);\n        \n        if (my_density > below_density && (is_movable(below_phase) || below_mat == MAT_AIR)) {\n            return below;\n        }\n        \n        // === DIAGONAL FALL (only if blocked below) ===\n        if (my_density <= below_density) {\n            // Try diagonals based on parity\n            if (parity == 0u) {\n                // Left first\n                if (x > 0u) {\n                    let diag = check_displace(x - 1u, y + 1u, my_density);\n                    if (diag != 0u) { return diag; }\n                }\n                if (x < GRID_WIDTH - 1u) {\n                    let diag = check_displace(x + 1u, y + 1u, my_density);\n                    if (diag != 0u) { return diag; }\n                }\n            } else {\n                // Right first\n                if (x < GRID_WIDTH - 1u) {\n                    let diag = check_displace(x + 1u, y + 1u, my_density);\n                    if (diag != 0u) { return diag; }\n                }\n                if (x > 0u) {\n                    let diag = check_displace(x - 1u, y + 1u, my_density);\n                    if (diag != 0u) { return diag; }\n                }\n            }\n        }\n    }\n    \n    // === HORIZONTAL SPREAD (fluids only) ===\n    if (is_fluid(phase)) {\n        if (parity == 0u) {\n            if (x > 0u) {\n                let left = check_displace(x - 1u, y, my_density);\n                if (left != 0u) { return left; }\n            }\n            if (x < GRID_WIDTH - 1u) {\n                let right = check_displace(x + 1u, y, my_density);\n                if (right != 0u) { return right; }\n            }\n        } else {\n            if (x < GRID_WIDTH - 1u) {\n                let right = check_displace(x + 1u, y, my_density);\n                if (right != 0u) { return right; }\n            }\n            if (x > 0u) {\n                let left = check_displace(x - 1u, y, my_density);\n                if (left != 0u) { return left; }\n            }\n        }\n    }\n    \n    // === BUOYANCY (check above) ===\n    if (y > 0u) {\n        let above = grid_in[get_idx(x, y - 1u)];\n        let above_mat = get_material(above);\n        let above_density = density_of(above_mat);\n        let above_phase = get_phase(above);\n        \n        if (above_density > my_density && (is_movable(above_phase) || above_mat == MAT_AIR)) {\n            return above;\n        }\n    }\n    \n    return voxel;\n}\n\n@compute @workgroup_size(16, 16)\nfn tick(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let x = gid.x;\n    let y = gid.y;\n    if (x >= GRID_WIDTH || y >= GRID_HEIGHT) { return; }\n\n    let idx = get_idx(x, y);\n    let voxel = grid_in[idx];\n    let mat = get_material(voxel);\n    let phase = get_phase(voxel);\n\n    if (!is_movable(phase) && mat != MAT_AIR) {\n        grid_out[idx] = voxel;\n        return;\n    }\n\n    let my_density = density_of(mat);\n    grid_out[idx] = resolve_swap(x, y, voxel, phase, my_density);\n}"
  }
}